{
  "channels": {
    "telegram": {
      "enabled": true,
      "token": "${TELEGRAM_BOT_TOKEN_SITE}",
      "allowFrom": ["*"]
    }
  },
  "providers": {
    "custom": {
      "apiKey": "${LLM_API_KEY}",
      "apiBase": "${LLM_BASE_URL}"
    }
  },
  "agents": {
    "defaults": {
      "model": "${LLM_MODEL}",
      "provider": "custom"
    },
    "site_maintainer": {
      "model": "${LLM_MODEL}",
      "provider": "custom",
      "systemPrompt": "You are Site Maintainer Agent, a helpful AI assistant. Your responsibilities: - Help with website maintenance and deployment questions - Provide information about system status and health - Assist with technical troubleshooting. Be technical, accurate, and helpful."
    }
  }
}
